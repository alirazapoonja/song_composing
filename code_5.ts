let myName: string =
  "\t\n typescipt ke sau coding karke humne dekhe hain, \t\n \n Ek mein bhi error the (sau mein bhi error hain)"; // This saves the name with whitespace

console.log(myName); //Shows the name with whitespace
console.log(myName.trim()); // Shows the name without whitespace
